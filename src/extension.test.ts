import { describe, it } from "vitest";

describe("Extension Tests", () => {
  describe("default configuration", () => {
    it.todo("can match test files");
    it.todo("can match production files");
    it.todo("can determine test files related to production files");
    it.todo("can determine production files related to test files");
  });
});
